node_modules/
.next/
.env
.env.local
*.log
.DS_Store

# Local SQLite database (zero-config dev mode) — not committed, since it's
# generated by `npm run db:init && npm run db:seed` and is machine-specific.
local.db
local.db-journal
